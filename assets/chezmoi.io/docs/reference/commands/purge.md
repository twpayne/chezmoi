# `purge`

Remove chezmoi's configuration, state, and source directory, but leave the
target state intact.

## `-f`, `--force`

Remove without prompting.

!!! example

    ```console
    $ chezmoi purge
    $ chezmoi purge --force
    ```
