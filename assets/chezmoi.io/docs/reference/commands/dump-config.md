# `dump-config`

Dump the configuration.

## Common flags

### `-f`, `--format` `json`|`yaml`

Set the output format, `json` by default.

## Examples

!!! example

    ```console
    $ chezmoi dump-config
    ```
